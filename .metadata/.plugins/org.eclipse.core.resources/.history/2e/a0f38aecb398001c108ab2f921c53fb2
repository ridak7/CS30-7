
public class PersonalAcct extends Account
{
	double minBal = 100;
	double penalty = 2;
	public Account acc;


	public PersonalAcct(double pen, double bal) 
	{
		acc = new Account(bal, fName, lName, str, city, st, zip);
		
		double balance = super.getBalance();
				
		/*bal = super.getBalance();*/
		
		/*if (bal < minBal)
		{
			bal = bal - penalty;
		}*/
	
	}

}
